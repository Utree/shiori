import 'package:flutter/material.dart';

class AddContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'AddContent',
      home: Scaffold(
        appBar: AppBar(
          title: Text('AddContent'),
        ),
      ),
    );
  }
}
